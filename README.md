Build and Run

# The project uses _Maven_ for dependency management and building and TestNG for testing.

The project has two test suites with first suite testing general functionality of selenium site and the second testing identical selectors.

# Compile the project

mvn compile

# Run the tests

mvn test

# Package the application

mvn package
Requirements
Java 21
Maven 3.8+
